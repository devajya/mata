// AGENT-CTX: Imports custom jest-dom matchers (toBeInTheDocument, toHaveTextContent, etc.)
// Must be imported here (not in individual test files) so they're available globally.
import "@testing-library/jest-dom";
