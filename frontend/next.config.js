/** @type {import('next').NextConfig} */

// AGENT-CTX: Minimal config for walking skeleton. No rewrites, no image domains,
// no custom headers needed at this stage. Add only when a feature requires it.
const nextConfig = {}

module.exports = nextConfig
