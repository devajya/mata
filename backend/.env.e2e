# AGENT-CTX: E2E smoke test target URLs. No secrets — only public deployment URLs.
# This file IS safe to commit (unlike backend/.env which contains API keys).
# Loaded by `make test-e2e`. Override these if deployments move.

# Render backend (deployed in S1-T6)
E2E_API_URL=https://mata-ooui.onrender.com

# Vercel frontend (deployed in S1-T8)
# AGENT-CTX: Update this value if the Vercel project is renamed or transferred.
E2E_FRONTEND_URL=https://mata-devajyas-projects.vercel.app
